{# navigation/navigation.twig #}
<nav class="main-navigation">
	<ul>
		{% for item in menu_items %}
			<li>
				<a href="{{ item.url }}" {{ item.active ? 'class="active"' : '' }}>
					{{ item.title }}
				</a>
			</li>
		{% endfor %}
	</ul>
</nav>
